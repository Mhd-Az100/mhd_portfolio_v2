name: mhd_portfolio_v2
description: A new Flutter project.
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.1.0 <4.0.0"

dependencies:
  custom_mouse_cursor: ^1.1.0
  flutter:
    sdk: flutter
  flutter_animate: ^4.2.0+1
  flutter_bloc: ^8.1.3
  flutter_layout_grid: ^2.0.5
  flutter_screenutil: ^5.9.0
  flutter_svg: ^2.0.7
  glassmorphism: ^3.0.0
  google_fonts: ^5.1.0
  scrollable_positioned_list: ^0.3.8
  url_launcher: ^6.1.14

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/svg/.
    - assets/imgs/.
